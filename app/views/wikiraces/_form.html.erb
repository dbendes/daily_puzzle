<%= form_for(@wikirace) do |f| %>
  <% if @wikirace.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wikirace.errors.count, "error") %> prohibited this wikirace from being saved:</h2>

      <ul>
      <% @wikirace.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :start %><br>
    <%= f.text_field :start %>
  </div>
  <div class="field">
    <%= f.label :end %><br>
    <%= f.text_field :end %>
  </div>
  <div class="field">
    <%= f.label :racedate %><br>
    <%= f.date_select :racedate %>
  </div>
  <div class="field">
    <%= f.label :start_description %><br>
    <%= f.text_area :start_description %>
  </div>
  <div class="field">
    <%= f.label :end_description %><br>
    <%= f.text_area :end_description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
